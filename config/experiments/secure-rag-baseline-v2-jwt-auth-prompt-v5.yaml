# secure_rag_baseline_v2_jwt_auth_prompt_v5: rag_answer prompt A/B
# candidate, follow-up to the v3-vs-v4 comparison (2026-08-22).
#
# Identical to secure-rag-baseline-v2-jwt-auth.yaml (experiment_028, the
# control for this A/B) except:
#   - generation.prompt.version/path: v3 -> v5 (src/rag/prompts/templates/
#     rag_answer_v5.yaml). v5 is v4 with one added sentence on rule 9: do
#     not reproduce the literal "[REDACTED:SENSITIVE_FIELD]" marker text,
#     state in your own words that the value is unavailable. This closes
#     the one genuine behavioral gap found on the v3-vs-v4 deterministic
#     eval (v4 sometimes echoed the marker verbatim -- see
#     experiments/results/ for that comparison's record). v4 itself is
#     deliberately not the control here -- v3 is, so this A/B answers "does
#     the corrected prompt still hold v4's gains without the marker-echo
#     issue", not "v5 vs v4".
#
# No other field differs from experiment_028's config.
#
# Every component below is a "swap point": change `provider` (and its
# provider-specific block) to try a different embedding model, vector
# backend, chunker, reranker, or LLM without touching pipeline code.
#
# Secrets and host/port values are never hardcoded here: fields ending in
# `_env_var` name an environment variable that is resolved at load time by
# rag.config.Settings.

app:
  name: local-rag-system
  log_level: INFO

embedding:
  provider: sentence_transformers # swap point
  model_name: sentence-transformers/all-MiniLM-L6-v2
  device: cpu
  batch_size: 32
  normalize: true
  dimension: 384 # must match model output dim; used to size the pgvector column

vectorstore:
  provider: pgvector # swap point (future: chroma, faiss, ...)
  connection_env_var: DATABASE_URL # never hardcode host/port/credentials
  documents_table: documents
  chunks_table: chunks
  distance_metric: cosine

chunking:
  provider: structured_markdown # swap point: recursive_character | structured_markdown
  chunk_size: 500
  chunk_overlap: 50
  structured_markdown:
    table_row_group_size: 20
    max_atomic_block_chars: 2000

reranker:
  provider: none # swap point: none | cross_encoder | cohere
  cross_encoder:
    model_name: cross-encoder/ms-marco-MiniLM-L-6-v2
  cohere:
    model_name: rerank-english-v3.0
    api_key_env_var: COHERE_API_KEY

generation:
  provider: ollama # swap point
  model_name: qwen2.5:3b
  base_url_env_var: OLLAMA_BASE_URL
  temperature: 0.0 # matches experiment_028
  max_tokens: 512
  seed: 42 # matches experiment_028
  prompt:
    id: rag_answer
    version: v5 # THE single variable this experiment changes -- see header note
    path: src/rag/prompts/templates/rag_answer_v5.yaml

judge:
  provider: openai # swap point: openai | anthropic | ollama (ollama = local, for experimentation only)
  temperature: 0.0
  max_tokens: 1024
  openai:
    model_name: gpt-4o-mini
    api_key_env_var: OPENAI_API_KEY
  anthropic:
    model_name: claude-haiku-4-5-20251001
    api_key_env_var: ANTHROPIC_API_KEY
  ollama:
    model_name: llama3.1:8b # deliberately distinct from generation.model_name

mlflow:
  enabled: true
  tracking_uri: "sqlite:///mlflow.db" # swap point: local sqlite file | a real MLflow tracking server URL
  experiment_name: local-rag-system

retrieval:
  provider: hybrid # matches experiment_028
  candidate_k: 5
  generation_context_top_n: 3
  hybrid:
    rrf_k: 60
  relationship_expansion:
    enabled: true # matches experiment_028
    include_parent: true
    include_neighbors: true
    max_related_elements: 3

ingestion:
  supported_extensions: [".pdf", ".docx", ".html", ".htm", ".txt", ".md"]

vision:
  provider: none # hosted vision OFF (not this milestone)

security:
  authorization:
    enabled: true # unchanged from experiment_028
    cross_tenant_support_roles: ["techfusion_support"]
  field_redaction:
    enabled: true # unchanged from experiment_028
  auth:
    enabled: true # unchanged from experiment_028
    insecure_dev_mode: false # production-shaped: no body-trusted fallback
    ingest_roles: ["security_admin", "system_admin", "techfusion_support"]
    jwt:
      algorithm: HS256
      secret_env_var: JWT_HS256_SECRET
      public_key_path_env_var: JWT_PUBLIC_KEY_PATH
      issuer: null
      audience: null
      leeway_seconds: 30
      required_claims: ["sub", "tenant_id", "roles"]
  dos_limits:
    max_query_length: 2000
    max_top_k: 20
    max_filters_bytes: 4096
    max_upload_bytes: 26214400
  rate_limit:
    enabled: false # not the focus of this A/B; see docs/architecture.md's documented limitation
    requests_per_minute: 60
    key: tenant
  egress_policy:
    enabled: false # gates hosted RAGAS judge calls only; run explicitly for this A/B's RAGAS pass, not left on
    block_unredacted_sensitive_fields: true
    require_authoritative_trust: false
    blocked_tenant_ids: []
    classification_policy:
      public: true
      internal: true
      confidential: false
      restricted: false
